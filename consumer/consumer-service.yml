apiVersion: v1
kind: Service
metadata:
  name: lims-consumer
  namespace: leaf-image-management-system
spec:
  type: ClusterIP
  selector:
    app: lims-consumer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
